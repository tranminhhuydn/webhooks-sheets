<style>
.tooltip1 {
  position: relative;
  display: inline-block;
}

.tooltip1 .tooltiptext {
  visibility: hidden;
  width: 140px;
  background-color: #555;
  color: #fff;
  text-align: center;
  border-radius: 6px;
  padding: 5px;
  position: absolute;
  z-index: 1;
  bottom: 150%;
  left: 50%;
  margin-left: -75px;
  opacity: 0;
  transition: opacity 0.3s;
}

.tooltip1 .tooltiptext::after {
  content: "";
  position: absolute;
  top: 100%;
  left: 50%;
  margin-left: -5px;
  border-width: 5px;
  border-style: solid;
  border-color: #555 transparent transparent transparent;
}

.tooltip1:hover .tooltiptext {
  visibility: visible;
  opacity: 1;
} 
</style>
<a href="/project/"><h1>Project</h1></a>
<%=project[1]%>
<h1>Members</h1>

<div class="container text-center">
    <div class="row card-header">
        <div class="col-6">email</div>
        <div class="col-2">status</div>
        <div class="col-2">Secret</div>
    </div>
    <% data.map((e,i)=>{
       
        %>
        <div class="row">
            <div class="col-6"><%=e[1]%></div>
            <div class="col-2"><%=e[2]%></div>
            <div class="col-2 tooltip1">
                <% if (user.email==e[1]){%> 
                <input type="hidden" value="<%=e[4]%>" id="mySecret">
                <!-- <div class=""> -->
                    <button class="btn btn-secondary" onclick="toClipboard()" onmouseout="outFunc()">
                        <span class="tooltiptext" id="myTooltip">Copy to clipboard</span>
                        <span>Copy</span>
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-clipboard" viewBox="0 0 16 16">
                            <path d="M4 1.5H3a2 2 0 0 0-2 2V14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V3.5a2 2 0 0 0-2-2h-1v1h1a1 1 0 0 1 1 1V14a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V3.5a1 1 0 0 1 1-1h1v-1z"/>
                            <path d="M9.5 1a.5.5 0 0 1 .5.5v1a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5v-1a.5.5 0 0 1 .5-.5h3zm-3-1A1.5 1.5 0 0 0 5 1.5v1A1.5 1.5 0 0 0 6.5 4h3A1.5 1.5 0 0 0 11 2.5v-1A1.5 1.5 0 0 0 9.5 0h-3z"/>
                        </svg>
                    </button>
                <!-- </div> -->
                <!-- <button class="btn btn-secondary" onclick="toClipboard()">Copy text</button> -->
                <%} %>
            </div>
        </div>
        <%})%>
</div>
<script>
    function toClipboard() {
        // Get the text field
        var copyText = document.getElementById("mySecret");

        // Select the text field
        //copyText.select();
        //copyText.setSelectionRange(0, 99999); // For mobile devices

        // Copy the text inside the text field
        navigator.clipboard.writeText(copyText.value);

        // Alert the copied text
        //alert("Copied the text: " + copyText.value);
        var tooltip = document.getElementById("myTooltip");
        tooltip.innerHTML = "Copied" // + copyText.value;
    }
    function outFunc() {
        var tooltip = document.getElementById("myTooltip");
        tooltip.innerHTML = "Copy to clipboard";
    }
</script>